(function () {    'use strict';    angular        .module('App')        .service('TeacherService', ['$http', '$location', TeacherService]);    function TeacherService ($http, $location) {            this.saveUrl = '/teachers/save',            this.redirectUrl = '/teacher-list',            this.errMsgs = {                name: 'Имя обязательно для ввода',                phone: 'Телефон обязателен для ввода',                gender: 'Пол обязателен для ввода'            };            this.teacher = {                name: '',                phone: '',                gender: 1            };            this.save = function (teacher) {                var self=this;                $http.post(self.saveUrl, JSON.stringify(teacher))                    .success(function (data) {                        if (data.success) {                            $location.url(self.redirectUrl);                        }                    })                    .error(function (data) {                        console.dir(data);                    });            }    }})();